import { useConversationMessages, } from "./useConversationMessages.js";
export function AgentThread({ children, ...options }) {
    const thread = useConversationMessages(options);
    return children(thread);
}
