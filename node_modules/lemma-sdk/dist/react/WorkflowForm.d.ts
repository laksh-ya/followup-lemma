import type { ReactNode } from "react";
import { type UseWorkflowFormOptions, type UseWorkflowFormResult } from "./useWorkflowForm.js";
export interface WorkflowFormProps extends UseWorkflowFormOptions {
    children: (form: UseWorkflowFormResult) => ReactNode;
}
export declare function WorkflowForm({ children, ...options }: WorkflowFormProps): ReactNode;
