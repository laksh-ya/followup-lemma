import type { GeneratedClientAdapter } from "../generated.js";
import type { SurfaceUpsertRequest } from "../openapi_client/models/SurfaceUpsertRequest.js";
/**
 * Agent surfaces, addressed by platform (a surface is unique per pod+platform).
 *
 * One `upsert` write covers create, config/channel edits, account and
 * credential changes, and enable/disable (via `is_enabled`). `delete` removes
 * the surface and frees its account for reuse. `setup` merges live readiness,
 * admin-consent, and the platform checklist into one read.
 */
export declare class PodSurfacesNamespace {
    private readonly client;
    constructor(client: GeneratedClientAdapter);
    list(podId: string, options?: {
        limit?: number;
        pageToken?: string;
        cursor?: string;
    }): Promise<import("../types.js").AgentSurfaceListResponse>;
    upsert(podId: string, platform: string, payload: SurfaceUpsertRequest): Promise<any>;
    get(podId: string, platform: string): Promise<any>;
    delete(podId: string, platform: string): Promise<void>;
    setup(podId: string, platform: string): Promise<import("../types.js").SurfaceSetupResponse>;
    channels(podId: string, platform: string): Promise<import("../types.js").AvailableSurfaceChannelsResponse>;
}
