import type { AgentNodeConfig } from './AgentNodeConfig.js';
export type AgentNodeResponse = {
    config: AgentNodeConfig;
    id: string;
    label?: (string | null);
    position?: (Record<string, number> | null);
    type?: string;
};
