/**
 * Who may self-join an organization, ordered from closed to open.
 */
export declare enum OrganizationJoinPolicy {
    INVITE_ONLY = "INVITE_ONLY",
    EMAIL_DOMAIN = "EMAIL_DOMAIN",
    PUBLIC = "PUBLIC"
}
