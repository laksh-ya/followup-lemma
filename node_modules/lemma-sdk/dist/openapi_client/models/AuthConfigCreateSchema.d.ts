export type AuthConfigCreateSchema = {
    config_source?: string;
    connector_id: string;
    credential_config?: (Record<string, any> | null);
    name?: (string | null);
    provider?: string;
};
