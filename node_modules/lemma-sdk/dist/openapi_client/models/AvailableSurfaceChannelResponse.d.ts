/**
 * A channel/group the surface bot can be configured to respond in.
 */
export type AvailableSurfaceChannelResponse = {
    id: string;
    is_member?: (boolean | null);
    name?: (string | null);
};
